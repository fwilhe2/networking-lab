Source: networking-lab
Section: net
Priority: optional
Maintainer: Florian Wilhelm <fwilhelm.wgt@gmail.com>
# Targets Debian 13 (trixie), the current stable: it carries GTK 4.18,
# libadwaita 1.7, Vala 0.56 and VTE 0.80, which satisfy every floor in
# meson.build. Nothing here is available in bookworm.
Build-Depends:
 debhelper-compat (= 13),
 meson (>= 1.0.0),
 ninja-build,
 valac,
 pkgconf,
 libglib2.0-dev (>= 2.72),
 libglib2.0-dev-bin,
 libjson-glib-dev (>= 1.6),
 libgtk-4-dev (>= 4.12),
 libadwaita-1-dev (>= 1.5),
 libvte-2.91-gtk4-dev,
 gettext,
 desktop-file-utils,
 appstream,
Standards-Version: 4.7.0
Homepage: https://github.com/fwilhe2/networking-lab
Rules-Requires-Root: no

Package: networking-lab
Architecture: any
Depends:
 ${shlibs:Depends},
 ${misc:Depends},
 adwaita-icon-theme,
# Docker is a run-time dependency the application locates for itself and does
# without: the designer, the compiler and the export all work with no container
# runtime installed, and the run controls say why they are insensitive. That is
# a Recommends, not a Depends. Debian's docker-compose is the v2 Go
# implementation and installs the CLI plugin, so `docker compose` works — which
# is the syntax the generated file needs (>= 2.23.1, for inline configs).
Recommends:
 docker.io,
 docker-compose (>= 2.23.1),
Description: design and run container network labs
 Networking Lab draws a network of routers, switches, PCs and servers, compiles
 it to a docker compose file and boots it, so a topology can be sketched,
 started and logged into without leaving the window.
 .
 Routers run FRR, giving a vtysh CLI reachable from an embedded terminal;
 switches become docker bridge networks, and the addressing, gateways and
 point-to-point subnets are worked out from the drawing.
 .
 The compose file it produces is an ordinary one, so a lab is equally usable
 from a terminal, in CI, or on a machine that has never seen this application.
